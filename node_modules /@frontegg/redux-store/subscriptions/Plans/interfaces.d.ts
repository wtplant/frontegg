import { Plan } from '../general.interfaces';
export interface PlansState {
    loading: boolean;
    error: string | null;
    fetching: boolean;
    plans: Plan[];
}
export interface PlansActions {
    loadPlans: () => void;
}
