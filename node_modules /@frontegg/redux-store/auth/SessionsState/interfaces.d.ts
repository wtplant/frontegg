import { ISessionResponse } from '@frontegg/rest-api';
export interface SessionsState {
    loading: boolean;
    error?: any;
    currentSession?: ISessionResponse;
    sessions: ISessionResponse[];
}
