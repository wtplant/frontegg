import { PayloadAction } from '@reduxjs/toolkit';
import { GenerateStepUpResponse, ILoginResponseV3 } from '@frontegg/rest-api';
import { WithCallback } from '../../interfaces';
import { IGenerateStepUpSession } from './interfaces';
/**
 * Generate step up session
 * Logout on error and redirecting to the login page
 * @param payload.maxAge - The max age of the session in seconds
 * @param payload.callback - The callback function to be called after the request is done
 */
export declare function generateStepUpSession({ payload: { maxAge, callback }, }: PayloadAction<WithCallback<IGenerateStepUpSession>>): Generator<import("redux-saga/effects").CallEffect<void> | import("redux-saga/effects").PutEffect<{
    payload: Partial<import("./interfaces").StepUpState>;
    type: string;
}> | import("redux-saga/effects").CallEffect<ILoginResponseV3 | GenerateStepUpResponse>, void, ILoginResponseV3 | GenerateStepUpResponse>;
