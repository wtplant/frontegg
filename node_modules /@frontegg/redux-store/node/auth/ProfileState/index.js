"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.profileState = exports.profileReducers = exports.profileActions = void 0;
var _toolkit = require("@reduxjs/toolkit");
var _utils = require("../utils");
var _constants = require("../../constants");
const profileState = {
  loading: false,
  error: null
};
exports.profileState = profileState;
const reducers = {
  setProfileState: (0, _utils.typeReducerForKey)('profileState'),
  resetProfileState: (0, _utils.resetStateByKey)('profileState', {
    profileState
  })
};
exports.profileReducers = reducers;
const actions = {
  loadProfile: (0, _toolkit.createAction)(`${_constants.authStoreName}/loadProfile`),
  saveProfile: (0, _toolkit.createAction)(`${_constants.authStoreName}/saveProfile`, payload => ({
    payload
  })),
  changePassword: (0, _toolkit.createAction)(`${_constants.authStoreName}/changePassword`, payload => ({
    payload
  }))
};

/**
 *  To be used for actions types after dispatch, and should contains
 *  the reducers and actions as standalone function
 */
exports.profileActions = actions;
// noinspection JSUnusedLocalSymbols
/**
 *  if you see error in matcher that's mean the DispatchAction does not
 *  contains the same functions in reducers and actions
 */
const Matcher = {};