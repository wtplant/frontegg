"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.sessionsState = exports.sessionsReducers = exports.sessionsActions = void 0;
var _toolkit = require("@reduxjs/toolkit");
var _utils = require("../utils");
var _constants = require("../../constants");
const sessionsState = {
  loading: false,
  sessions: []
};
exports.sessionsState = sessionsState;
const reducers = {
  setSessionsState: (0, _utils.typeReducerForKey)('sessionsState'),
  resetSessionsState: (0, _utils.resetStateByKey)('sessionsState', {
    sessionsState
  })
};
exports.sessionsReducers = reducers;
const actions = {
  loadUserSessions: (0, _toolkit.createAction)(`${_constants.authStoreName}/loadUserSessions`),
  loadCurrentUserSession: (0, _toolkit.createAction)(`${_constants.authStoreName}/loadCurrentUserSession`),
  deleteUserSession: (0, _toolkit.createAction)(`${_constants.authStoreName}/deleteUserSession`, payload => ({
    payload
  })),
  deleteAllUserSessions: (0, _toolkit.createAction)(`${_constants.authStoreName}/deleteAllUserSessions`),
  revokeUserSessions: (0, _toolkit.createAction)(`${_constants.authStoreName}/revokeUserSessions`, payload => ({
    payload
  }))
};

/**
 *  To be used for actions types after dispatch, and should contains
 *  the reducers and actions as standalone function
 */
exports.sessionsActions = actions;
// noinspection JSUnusedLocalSymbols
/**
 *  if you see error in matcher that's mean the DispatchAction does not
 *  contains the same functions in reducers and actions
 */
const Matcher = {};