import { FC } from 'react';
import { FronteggApp } from '@frontegg/js';
import { FronteggThemeOptions } from '@frontegg/types';
export declare type HistoryObject = {
    push: (path: string) => void;
    replace: (path: string) => void;
};
declare type QueryKeeperWrapperProps = {
    history: HistoryObject;
};
export declare const QueryKeeperWrapper: FC<QueryKeeperWrapperProps>;
declare type AlwaysRenderInProviderProps = {
    isExternalHistory: boolean;
    app: FronteggApp;
    themeOptions?: FronteggThemeOptions;
    history: HistoryObject;
};
export declare const AlwaysRenderInProvider: ({ isExternalHistory, app, themeOptions, history, }: AlwaysRenderInProviderProps) => JSX.Element;
export {};
