export interface PasswordStrengthLocalization {
    passwordStrength: {
        veryWeak: string;
        weak: string;
        good: string;
        strong: string;
        veryStrong: string;
    };
}
