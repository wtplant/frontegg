import { EntitlementResult } from '../types';
export declare function getResult(entitlementResults: EntitlementResult[]): EntitlementResult;
export declare function shouldContinue(entitlementResults: EntitlementResult[]): boolean;
